## Usage

The parser accepts an ELF file as a command-line argument:

./FileParser /bin/ls

You can change the file by replacing `/bin/ls` with the path
of any ELF file you want to analyze.

Examples:

./FileParser /bin/bash
./FileParser /usr/bin/grep
./FileParser ./my_program
